# 定时任务管理 UI 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增「定时任务」管理页，让用户查看/启停/删除自己的 cron 任务（与定时提醒配套）。

**Architecture:** 后端 `CronService.set_job_enabled()` 新方法 + `deeptutor/api/routers/cron.py`（GET/DELETE/PATCH 三个端点，owner 隔离 local-admin）；前端 `web/app/(utility)/tasks/page.tsx` 任务管理页 + 侧边栏入口 + `web/lib/cron-api.ts`。

**Tech Stack:** Python FastAPI, Next.js, react-i18next。

**Spec:** `docs/specs/cron-ui-design.md`（已提交 `265594f2`）

---

### Task 1: CronService.set_job_enabled + 测试

**Files:**
- Modify: `deeptutor/services/cron/service.py`
- Test: `tests/services/test_cron_service.py`（或新建）

- [ ] **Step 1: 读现有 cron service 测试**

Check if `tests/services/test_cron_service.py` (or similar) exists. If yes, read its patterns. If no cron service test exists, create one.

- [ ] **Step 2: 写失败测试**

Append (or create) `tests/services/test_cron_service.py`:

```python
"""CronService — job store + scheduler."""

from __future__ import annotations

from deeptutor.services.cron import CronOwner, CronSchedule, CronService


def _service(tmp_path) -> CronService:
    return CronService(store_path=tmp_path / "jobs.json")


def test_set_job_enabled_toggles_persisted(tmp_path):
    s = _service(tmp_path)
    job = s.add_job(
        name="r",
        message="提醒练标注",
        schedule=CronSchedule(kind="every", every_seconds=30),
        owner=CronOwner(kind="chat", session_id="s1"),
    )
    assert job.enabled is True
    assert s.set_job_enabled(job.id, False) is True
    # persisted + re-read
    s2 = _service(tmp_path)
    assert s2.get_job(job.id).enabled is False
    assert s2.set_job_enabled(job.id, True) is True


def test_set_job_enabled_missing_or_wrong_owner(tmp_path):
    s = _service(tmp_path)
    assert s.set_job_enabled("nope", False) is False
    job = s.add_job(
        name="r",
        message="x",
        schedule=CronSchedule(kind="every", every_seconds=30),
        owner=CronOwner(kind="chat", session_id="s1", user_id="u1"),
    )
    # wrong owner key
    assert s.set_job_enabled(job.id, False, owner_key="chat:other") is False
    assert s.set_job_enabled(job.id, False, owner_key="chat:u1") is True
```

Run: `cd "D:\自己\git帅\-deeptutor-agent-"; $env:PYTHONIOENCODING="utf-8"; python -m pytest tests/services/test_cron_service.py -v 2>&1 | Select-Object -Last 6`
Expected: FAIL (set_job_enabled not defined)

- [ ] **Step 3: 实现 set_job_enabled**

Add to `deeptutor/services/cron/service.py` (CronService class, after `cancel_job`):

```python
def set_job_enabled(self, job_id: str, enabled: bool, *, owner_key: str | None = None) -> bool:
    """Toggle a job's enabled flag (persisted)."""
    self._load()
    job = self._jobs.get(job_id)
    if job is None:
        return False
    if owner_key is not None and job.owner.key != owner_key:
        return False
    job.enabled = bool(enabled)
    self._save()
    self._wake.set()
    return True
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "D:\自己\git帅\-deeptutor-agent-"; $env:PYTHONIOENCODING="utf-8"; python -m pytest tests/services/test_cron_service.py -v 2>&1 | Select-Object -Last 6`
Expected: PASS.

- [ ] **Step 5: Ruff + Commit**

Ruff: `python -m ruff check deeptutor/services/cron/service.py tests/services/test_cron_service.py`

```bash
git add deeptutor/services/cron/service.py tests/services/test_cron_service.py
git commit -m "feat: CronService.set_job_enabled 启停任务 (持久化)"
```

---

### Task 2: 后端 cron REST 路由

**Files:**
- Create: `deeptutor/api/routers/cron.py`
- Modify: `deeptutor/api/main.py`（挂载）
- Test: `tests/api/test_cron_router.py`

- [ ] **Step 1: 读现有路由模式**

Read `deeptutor/api/routers/standards.py` (simple router) and `deeptutor/api/main.py` (how routers are mounted with `_auth`). Read `deeptutor/services/cron/__init__.py` exports.

- [ ] **Step 2: 写失败测试**

Create `tests/api/test_cron_router.py`:

```python
"""cron REST router — list/delete/toggle timed tasks."""

from __future__ import annotations

import pytest


@pytest.mark.asyncio
async def test_list_jobs_empty():
    from deeptutor.api.routers.cron import list_jobs

    result = await list_jobs()
    assert "jobs" in result
    assert isinstance(result["jobs"], list)


@pytest.mark.asyncio
async def test_toggle_and_delete_job():
    from deeptutor.api.routers.cron import delete_job, list_jobs, toggle_job
    from deeptutor.services.cron import CronOwner, CronSchedule, get_cron_service

    service = get_cron_service()
    job = service.add_job(
        name="r", message="x",
        schedule=CronSchedule(kind="every", every_seconds=30),
        owner=CronOwner(kind="chat", session_id="s1"),
    )
    try:
        jobs = await list_jobs()
        assert any(j["id"] == job.id for j in jobs["jobs"])
        toggled = await toggle_job(job.id, {"enabled": False})
        assert toggled["ok"] is True
        assert toggled["enabled"] is False
        deleted = await delete_job(job.id)
        assert deleted["ok"] is True
    finally:
        service.cancel_job(job.id)
```

NOTE: read `deeptutor/api/routers/cron.py` after Step 3 — the endpoints `list_jobs`/`delete_job`/`toggle_job` may have different signatures (e.g. `toggle_job(job_id, payload)` as a FastAPI route with Pydantic body). Adapt the test to the actual signatures. If the router uses a Pydantic model for the PATCH body, pass the dict (FastAPI TestClient serializes it; for direct function calls, pass the model or a dict matching `payload.enabled` — read the implementation).

Run: `cd "D:\自己\git帅\-deeptutor-agent-"; $env:PYTHONIOENCODING="utf-8"; python -m pytest tests/api/test_cron_router.py -v 2>&1 | Select-Object -Last 6`
Expected: FAIL (ImportError)

- [ ] **Step 3: 实现 cron.py**

Create `deeptutor/api/routers/cron.py`:

```python
"""Cron job management REST API (list/delete/toggle)."""

from __future__ import annotations

from typing import Any

from fastapi import APIRouter
from pydantic import BaseModel

from deeptutor.services.cron import get_cron_service

router = APIRouter()


class JobToggleRequest(BaseModel):
    enabled: bool


def _owner_key() -> str:
    """Chat jobs are scoped to the local admin in single-user runs."""
    return "chat:local-admin"


def _job_to_dict(job) -> dict[str, Any]:
    return {
        "id": job.id,
        "name": job.name,
        "message": job.message,
        "schedule": {
            "kind": job.schedule.kind,
            "every_seconds": job.schedule.every_seconds,
            "at_ms": job.schedule.at_ms,
            "expr": job.schedule.expr,
            "tz": job.schedule.tz,
        },
        "enabled": job.enabled,
        "next_run_at_ms": job.state.next_run_at_ms,
        "last_status": job.state.last_status,
        "last_error": job.state.last_error,
    }


@router.get("/cron/jobs")
async def list_jobs() -> dict[str, Any]:
    service = get_cron_service()
    return {"jobs": [_job_to_dict(j) for j in service.list_jobs(owner_key=_owner_key())]}


@router.delete("/cron/jobs/{job_id}")
async def delete_job(job_id: str) -> dict[str, Any]:
    service = get_cron_service()
    if service.cancel_job(job_id, owner_key=_owner_key()):
        return {"ok": True}
    return {"ok": False, "error": "任务不存在或无权删除"}


@router.patch("/cron/jobs/{job_id}")
async def toggle_job(job_id: str, payload: JobToggleRequest) -> dict[str, Any]:
    service = get_cron_service()
    if service.set_job_enabled(job_id, payload.enabled, owner_key=_owner_key()):
        return {"ok": True, "enabled": payload.enabled}
    return {"ok": False, "error": "任务不存在或无权操作"}
```

- [ ] **Step 4: 挂载到 main.py**

In `deeptutor/api/main.py`, add import + include (follow the standards router pattern):
```python
from deeptutor.api.routers import cron as cron_router
...
app.include_router(
    cron_router.router,
    prefix="/api/v1",
    tags=["cron"],
    dependencies=_auth,
)
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd "D:\自己\git帅\-deeptutor-agent-"; $env:PYTHONIOENCODING="utf-8"; python -m pytest tests/api/test_cron_router.py -v 2>&1 | Select-Object -Last 6`
Expected: PASS. Adjust the test to actual signatures if needed (read the implementation).

- [ ] **Step 6: Ruff + Commit**

Ruff: `python -m ruff check deeptutor/api/routers/cron.py deeptutor/api/main.py tests/api/test_cron_router.py`

```bash
git add deeptutor/api/routers/cron.py deeptutor/api/main.py tests/api/test_cron_router.py
git commit -m "feat: cron REST API (GET/DELETE/PATCH jobs, owner隔离)"
```

---

### Task 3: 前端定时任务页

**Files:**
- Create: `web/lib/cron-api.ts`
- Create: `web/app/(utility)/tasks/page.tsx`
- Modify: `web/components/sidebar/SidebarShell.tsx`（Secondary NAV 加「定时任务」）

- [ ] **Step 1: API 客户端**

Create `web/lib/cron-api.ts`:

```ts
import { apiFetch, apiUrl } from "@/lib/api";

export type CronSchedule = {
  kind: string;
  every_seconds?: number | null;
  at_ms?: number | null;
  expr?: string | null;
  tz?: string | null;
};

export type CronJob = {
  id: string;
  name: string;
  message: string;
  schedule: CronSchedule;
  enabled: boolean;
  next_run_at_ms?: number | null;
  last_status?: string | null;
  last_error?: string | null;
};

export async function getCronJobs(): Promise<{ jobs: CronJob[] }> {
  const res = await apiFetch(apiUrl("/api/v1/cron/jobs"));
  if (!res.ok) throw new Error(`Failed to load cron jobs: ${res.status}`);
  return res.json();
}

export async function deleteCronJob(id: string): Promise<{ ok: boolean }> {
  const res = await apiFetch(apiUrl(`/api/v1/cron/jobs/${id}`), { method: "DELETE" });
  if (!res.ok) throw new Error(`Failed to delete cron job: ${res.status}`);
  return res.json();
}

export async function toggleCronJob(id: string, enabled: boolean): Promise<{ ok: boolean }> {
  const res = await apiFetch(apiUrl(`/api/v1/cron/jobs/${id}`), {
    method: "PATCH",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ enabled }),
  });
  if (!res.ok) throw new Error(`Failed to toggle cron job: ${res.status}`);
  return res.json();
}
```

NOTE: check `web/lib/api.ts` for the exact `apiFetch` signature (method/headers/body handling) and mirror it — earlier files (book-api, knowledge-api) use `apiFetch(apiUrl(...), init)`.

- [ ] **Step 2: 定时任务页**

Create `web/app/(utility)/tasks/page.tsx`:

```tsx
"use client";

import { useEffect, useState } from "react";
import { Clock, Trash2 } from "lucide-react";
import { getCronJobs, deleteCronJob, toggleCronJob, type CronJob } from "@/lib/cron-api";

function describeSchedule(job: CronJob): string {
  const s = job.schedule;
  if (s.kind === "at" && s.at_ms) return `一次 · ${new Date(s.at_ms).toLocaleString()}`;
  if (s.kind === "every" && s.every_seconds) return `每 ${s.every_seconds} 秒`;
  if (s.kind === "cron" && s.expr) return `cron \`${s.expr}\`${s.tz ? ` (${s.tz})` : ""}`;
  return s.kind;
}

function formatNextRun(ms: number | null | undefined): string {
  if (!ms) return "—";
  return new Date(ms).toLocaleString();
}

export default function TasksPage() {
  const [jobs, setJobs] = useState<CronJob[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(false);

  const load = () => {
    getCronJobs()
      .then((r) => setJobs(r.jobs))
      .catch(() => setError(true))
      .finally(() => setLoading(false));
  };

  useEffect(() => { load(); }, []);

  async function handleToggle(job: CronJob, enabled: boolean) {
    try {
      await toggleCronJob(job.id, enabled);
      load();
    } catch {}
  }

  async function handleDelete(job: CronJob) {
    try {
      await deleteCronJob(job.id);
      load();
    } catch {}
  }

  if (loading) {
    return <div className="mx-auto max-w-3xl px-6 py-10 text-sm text-[var(--muted-foreground)]">加载中...</div>;
  }

  return (
    <div className="mx-auto max-w-3xl space-y-4 px-6 py-8">
      <div className="flex items-center gap-2">
        <Clock className="h-5 w-5 text-blue-500" />
        <div>
          <h1 className="text-lg font-bold">定时任务</h1>
          <p className="text-sm text-[var(--muted-foreground)]">查看和管理学习提醒等定时任务</p>
        </div>
      </div>
      {error && <p className="text-sm text-red-500">加载失败</p>}
      {jobs.length === 0 && (
        <p className="text-sm text-[var(--muted-foreground)]">
          暂无定时任务——和教练说"30 秒后提醒我练标注"即可创建。
        </p>
      )}
      {jobs.map((job) => (
        <div key={job.id} className="rounded-2xl border border-[var(--border)] bg-[var(--card)] p-4">
          <div className="flex items-center gap-2">
            <span className="text-sm font-semibold">{job.name || job.id}</span>
            <span className={`rounded px-1.5 py-0.5 text-[10px] ${job.enabled ? "bg-green-500/10 text-green-600" : "bg-[var(--border)] text-[var(--muted-foreground)]"}`}>
              {job.enabled ? "启用中" : "已停用"}
            </span>
            <div className="ml-auto flex items-center gap-2">
              <button
                type="button"
                onClick={() => handleToggle(job, !job.enabled)}
                className="rounded border border-[var(--border)] px-2 py-1 text-[11px] text-[var(--muted-foreground)] hover:bg-[var(--muted)]"
              >
                {job.enabled ? "停用" : "启用"}
              </button>
              <button
                type="button"
                onClick={() => handleDelete(job)}
                aria-label="删除任务"
                className="rounded border border-[var(--border)] p-1 text-[var(--muted-foreground)] hover:bg-red-500/10 hover:text-red-500"
              >
                <Trash2 className="h-3.5 w-3.5" />
              </button>
            </div>
          </div>
          <div className="mt-2 space-y-1 text-xs text-[var(--muted-foreground)]">
            <p>调度：{describeSchedule(job)}</p>
            <p>下次运行：{formatNextRun(job.next_run_at_ms)}</p>
            {job.last_status && <p>上次：{job.last_status}{job.last_error ? ` · ${job.last_error}` : ""}</p>}
          </div>
          <p className="mt-2 truncate text-xs text-[var(--muted-foreground)]/70">{job.message}</p>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: 侧边栏入口**

In `web/components/sidebar/SidebarShell.tsx`, add to SECONDARY_NAV (next to 标注规范):
- label 定时任务, href /tasks, icon Clock (import from lucide-react)

- [ ] **Step 4: tsc + Commit**

Run: `cd "D:\自己\git帅\-deeptutor-agent-\web"; npx tsc --noEmit` → 0 errors

```bash
git add web/lib/cron-api.ts "web/app/(utility)/tasks/page.tsx" web/components/sidebar/SidebarShell.tsx
git commit -m "feat: 定时任务管理页 (查看/启停/删除) + 侧边栏入口"
```

---

### Task 4: 验证

**Files:** none

- [ ] **Step 1: 后端测试**

Run: `cd "D:\自己\git帅\-deeptutor-agent-"; $env:PYTHONIOENCODING="utf-8"; python -m pytest tests/api/test_cron_router.py tests/services/test_cron_service.py -q 2>&1 | Select-Object -Last 3`
Expected: PASS.

- [ ] **Step 2: 前端 tsc + build**

Run: `cd "D:\自己\git帅\-deeptutor-agent-\web"; npx tsc --noEmit` → 0 errors; `Remove-Item Env:HTTP_PROXY,Env:HTTPS_PROXY -ErrorAction SilentlyContinue; npx next build 2>&1 | Select-Object -Last 4` → succeeds

- [ ] **Step 3: Playwright 冒烟**

With backend (8001) + frontend (3782):
1. In a chat, ask Coach "帮我设个定时任务，30秒后提醒我练标注" to register a job
2. Navigate to `/tasks` → the job shows (name, 每 30 秒, 下次运行, 启用中)
3. Click 停用 → job shows 已停用
4. Click 删除 → job disappears (empty state)
5. Screenshot for record

- [ ] **Step 4: 提交修复（如有）**

If smoke found issues, fix + commit. Otherwise no commit.

---

## Self-Review

**1. Spec coverage:**
- §3 后端 cron.py + set_job_enabled → Task 1 + Task 2
- §4 前端 tasks 页 → Task 3
- §5 测试 → Task 4
✅ 全覆盖

**2. Placeholder scan:** 所有步骤含具体代码/命令。Task 2 的 Pydantic body 用法明确（测试需读实现调整签名）。✅

**3. Type consistency:** `CronJob`/`CronSchedule` 类型 Task 3 Step 1 定义，Step 2/3 引用一致；后端 `_job_to_dict` 返回字段（id/name/message/schedule/enabled/next_run_at_ms/last_status/last_error）与前端 CronJob 一致；`set_job_enabled(job_id, enabled, owner_key)` 签名 Task 1/2 一致。✅

**已知风险（沿用 spec §7）：**
1. owner_key 硬编码 chat:local-admin——单机演示 OK，多用户后续
2. `set_job_enabled` 是 service 新方法，测试覆盖
3. 停用任务后 scheduler 已过滤 enabled，不触发
