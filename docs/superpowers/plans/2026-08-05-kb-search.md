# 议题③ kb_search 知识库检索 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `kb_search` 工具，让 Coach 在 `data/user/workspace/annotation_kb/`（60 篇标注知识文档，6 大类）里精确检索，返回命中文档标题 + 片段 + 来源；无命中明确返回"知识库未收录此内容"。并把 `annotation_kb` 纳入 git（clone 即用）。

**Architecture:** 纯文件检索（关键词打分），**不依赖 LLM**（快、可测）。`search_kb` 纯函数扫描 annotation_kb 下 md 文件，按 query token 出现次数打分取 top-k，返回 `{title, category, snippet, source}`。`KbSearchTool` 包装成 BaseTool（同 route_input 模式）。category 可限定 6 大类。

**Tech Stack:** Python 3.11+ / pytest / pathlib / DeepTutor BaseTool 协议

**依赖设计文档:** `docs/superpowers/specs/2026-08-05-knowledge-base-design.md`

---

## File Structure

- Create: `deeptutor/tools/kb_search_tool.py` — `KbSearchTool` + `search_kb` 纯函数 + `CATEGORY_DIRS` 常量 + `KB_ROOT`
- Create: `tests/tools/test_kb_search_tool.py` — search_kb 纯函数测试 + execute 测试
- Modify: `deeptutor/tools/builtin/__init__.py` — 4 处注册
- Modify: `deeptutor/agents/_shared/tool_composition.py` — always_on 加 `"kb_search"`
- Modify: `deeptutor/services/persona/presets/annotation-coach/PERSONA.md` + 副本 — 「知识检索」节
- Git: `data/user/workspace/annotation_kb/` 强制纳入 git（`git add -f`）

---

### Task 1: `kb_search_tool.py`（search_kb 纯函数 + 工具类）

**Files:**
- Create: `deeptutor/tools/kb_search_tool.py`
- Test: `tests/tools/test_kb_search_tool.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/tools/test_kb_search_tool.py
import pytest

from deeptutor.tools.kb_search_tool import KB_ROOT, CATEGORY_DIRS, KbSearchTool, search_kb


def test_kb_root_exists_and_has_docs():
    assert KB_ROOT.exists()
    md_files = list(KB_ROOT.rglob("*.md"))
    assert len(md_files) >= 40  # 60 篇（含未来扩展，至少 40）


def test_categories_cover_six_domains():
    assert any("基础" in c for c in CATEGORY_DIRS)
    assert any("标准" in c for c in CATEGORY_DIRS)
    assert len(CATEGORY_DIRS) >= 5


def test_search_keyword_hits():
    hits = search_kb("遮挡", top_k=5)
    assert isinstance(hits, list) and hits
    first = hits[0]
    assert "title" in first and "snippet" in first and "source" in first and "category" in first
    assert "遮挡" in first["snippet"] or "遮挡" in first["title"]


def test_search_category_limited():
    std = next(c for c in CATEGORY_DIRS if "标准" in c)
    hits = search_kb("标注", category=std, top_k=5)
    if hits:
        assert all(h["category"] == std for h in hits)


def test_search_no_hit_returns_empty():
    hits = search_kb("zzzqqqxxxyyy_not_exist", top_k=5)
    assert hits == []


def test_top_k_respected():
    hits = search_kb("标注", top_k=3)
    assert len(hits) <= 3


async def test_execute_returns_hits():
    tool = KbSearchTool()
    result = await tool.execute(query="遮挡")
    assert result.success is True
    assert "遮挡" in result.content


async def test_execute_no_hit_message():
    tool = KbSearchTool()
    result = await tool.execute(query="zzzqqqxxxyyy_not_exist")
    assert result.success is True
    assert "知识库未收录此内容" in result.content


async def test_execute_empty_query_fails():
    tool = KbSearchTool()
    result = await tool.execute(query="   ")
    assert result.success is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/tools/test_kb_search_tool.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'deeptutor.tools.kb_search_tool'`

- [ ] **Step 3: Write minimal implementation**

```python
# deeptutor/tools/kb_search_tool.py
"""KbSearchTool — precise keyword search over the annotation knowledge base.

Scans ``data/user/workspace/annotation_kb/`` (60 docs, 6 categories) for query
tokens and returns the top-k matches with title / snippet / source. No LLM
involved — deterministic and unit-testable. No hits => explicit "not in KB".
"""

from __future__ import annotations

import re
from pathlib import Path
from typing import Any

from deeptutor.core.tool_protocol import BaseTool, ToolDefinition, ToolParameter, ToolResult

KB_ROOT = Path(__file__).resolve().parents[2] / "data" / "user" / "workspace" / "annotation_kb"

CATEGORY_DIRS: tuple[str, ...] = (
    "01-基础知识",
    "02-行业标准",
    "03-工具操作",
    "04-质量管控",
    "05-常见错误",
    "06-项目管理",
)


def _tokenize(text: str) -> list[str]:
    """Split into CJK bigrams + ascii words (lightweight keyword tokens)."""
    tokens: list[str] = []
    cjk = re.findall(r"[\u4e00-\u9fff]+", text.lower())
    for chunk in cjk:
        if len(chunk) == 1:
            tokens.append(chunk)
        else:
            tokens.extend(chunk[i : i + 2] for i in range(len(chunk) - 1))
            tokens.append(chunk)  # whole chunk as a token too
    tokens.extend(re.findall(r"[a-z0-9]+", text.lower()))
    return [t for t in tokens if len(t) >= 2]


def _snippet(text: str, tokens: list[str], width: int = 40) -> str:
    positions = [m.start() for t in tokens for m in re.finditer(re.escape(t), text)]
    if not positions:
        return text[: width * 2].replace("\n", " ")
    pos = positions[0]
    start = max(0, pos - width)
    end = min(len(text), pos + width * 2)
    snippet = text[start:end].replace("\n", " ")
    prefix = "…" if start > 0 else ""
    suffix = "…" if end < len(text) else ""
    return f"{prefix}{snippet}{suffix}"


def search_kb(query: str, *, category: str | None = None, top_k: int = 3) -> list[dict[str, Any]]:
    """Return top-k matches as {title, category, snippet, source}."""
    if not KB_ROOT.exists():
        return []
    tokens = _tokenize(query or "")
    if not tokens:
        return []
    target_dirs = [category] if category and category in CATEGORY_DIRS else list(CATEGORY_DIRS)
    scored: list[tuple[int, dict[str, Any]]] = []
    for cat_dir in target_dirs:
        cat_path = KB_ROOT / cat_dir
        if not cat_path.is_dir():
            continue
        for md in cat_path.glob("*.md"):
            text = md.read_text(encoding="utf-8", errors="ignore")
            score = sum(text.count(t) for t in tokens)
            if score <= 0:
                continue
            title_match = re.search(r"^#\s+(.+)$", text, re.MULTILINE)
            title = title_match.group(1).strip() if title_match else md.stem
            scored.append((score, {
                "title": title,
                "category": cat_dir,
                "snippet": _snippet(text, tokens),
                "source": str(md.relative_to(KB_ROOT)).replace("\\", "/"),
            }))
    scored.sort(key=lambda item: item[0], reverse=True)
    return [item[1] for item in scored[: max(1, top_k)]]


class KbSearchTool(BaseTool):
    """Search the annotation knowledge base for teaching evidence."""

    def get_definition(self) -> ToolDefinition:
        return ToolDefinition(
            name="kb_search",
            description=(
                "Search the annotation knowledge base (60 docs across 6 categories: "
                "基础知识/行业标准/工具操作/质量管控/常见错误/项目管理). Use when teaching a "
                "knowledge point or citing a standard. Returns top matching docs with "
                "snippet and source. Category can be limited for precision."
            ),
            parameters=[
                ToolParameter(
                    name="query",
                    type="string",
                    description="Search keywords (e.g. 遮挡, IOU, GB/T 41867).",
                    required=True,
                ),
                ToolParameter(
                    name="category",
                    type="string",
                    description="Optional category dir name to limit the search.",
                    required=False,
                    enum=list(CATEGORY_DIRS),
                ),
            ],
        )

    async def execute(self, **kwargs: Any) -> ToolResult:
        query = str(kwargs.get("query") or "").strip()
        if not query:
            return ToolResult(content="Error: query is required.", success=False)
        category = str(kwargs.get("category") or "").strip() or None
        hits = search_kb(query, category=category, top_k=3)
        if not hits:
            return ToolResult(
                content="知识库未收录此内容。可改为通用教学建议并注明非标准条款。",
                metadata={"kb_search": {"hits": [], "query": query}},
            )
        lines = [f"知识库命中（{len(hits)} 条）: "]
        for h in hits:
            lines.append(f"- 【{h['title']}】({h['category']})\n  {h['snippet']}\n  来源: {h['source']}")
        return ToolResult(
            content="\n".join(lines),
            metadata={"kb_search": {"hits": hits, "query": query}},
        )


__all__ = ["KbSearchTool", "search_kb", "CATEGORY_DIRS", "KB_ROOT"]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/tools/test_kb_search_tool.py -v`
Expected: PASS (all tests)

- [ ] **Step 5: Commit**

```bash
git add deeptutor/tools/kb_search_tool.py tests/tools/test_kb_search_tool.py
git commit -m "feat: kb_search 工具 (知识库关键词检索, 返回标题/片段/来源)"
```

---

### Task 2: 注册 `kb_search`（4 处 + always_on）

**Files:**
- Modify: `deeptutor/tools/builtin/__init__.py`
- Modify: `deeptutor/agents/_shared/tool_composition.py`

- [ ] **Step 1: 注册 4 处**

`deeptutor/tools/builtin/__init__.py`（verify_output 之后）：

```python
# 1) import 区（verify_output_tool import 之后）
from deeptutor.tools.kb_search_tool import KbSearchTool

# 2) BUILTIN_TOOL_TYPES 列表（VerifyOutputTool 之后）
    KbSearchTool,

# 3) CONFIGURABLE_BUILTIN_TOOL_NAMES 元组（"verify_output" 之后）
    "kb_search",

# 4) __all__ 列表（"VerifyOutputTool" 之后）
    "KbSearchTool",
```

`deeptutor/agents/_shared/tool_composition.py`（always_on tuple，`"verify_output"` 之后）：

```python
        "kb_search",
```

- [ ] **Step 2: 验证注册 + 测试**

Run:
```powershell
python -c "from deeptutor.tools.builtin import BUILTIN_TOOL_TYPES, CONFIGURABLE_BUILTIN_TOOL_NAMES, __all__; print('KbSearchTool' in [t.__name__ for t in BUILTIN_TOOL_TYPES], 'kb_search' in CONFIGURABLE_BUILTIN_TOOL_NAMES, 'KbSearchTool' in __all__)"
```
Expected: `True True True`

Run: `python -m pytest tests/tools/test_kb_search_tool.py -q`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add deeptutor/tools/builtin/__init__.py deeptutor/agents/_shared/tool_composition.py
git commit -m "chore: 注册 kb_search (builtin 4 处 + always_on)"
```

---

### Task 3: annotation_kb 纳入 git + PERSONA「知识检索」节

**Files:**
- Git: `data/user/workspace/annotation_kb/`（`git add -f`）
- Modify: `deeptutor/services/persona/presets/annotation-coach/PERSONA.md` + 副本

- [ ] **Step 1: annotation_kb 强制纳入 git**

```bash
git add -f data/user/workspace/annotation_kb/
git commit -m "chore: annotation_kb 60 篇知识文档纳入 git (clone 即用)"
```

（`data/` 被 gitignore，必须 `git add -f`。推送后验证 `git ls-files data/user/workspace/annotation_kb | Measure-Object` ≥ 60。）

- [ ] **Step 2: PERSONA 加「知识检索」节（输入分诊节后）**

```markdown
## 知识检索（教学依据）

讲知识点/规范时，先 `kb_search` 查知识库（60 篇，6 大类），返回命中标题+片段+来源。
- 命中 → 依据片段教学，引用带来源（如 `〔规范: 文档§章节〕` 或知识库来源路径）。
- 未命中 → 明说"知识库未收录此内容"，改用通用教学建议并注明非标准条款。
- 需要精确规范时用 `category` 限定（行业标准/常见错误等）。
```

- [ ] **Step 3: 同步运行时副本 + 校验 + Commit**

Run（PowerShell）:
```powershell
Copy-Item deeptutor/services/persona/presets/annotation-coach/PERSONA.md data/user/workspace/personas/annotation-coach/PERSONA.md -Force
python -c "from pathlib import Path; p=Path('deeptutor/services/persona/presets/annotation-coach/PERSONA.md'); t=p.read_text(encoding='utf-8'); assert t.startswith('---') and '知识检索' in t; print('OK')"
```
Expected: `OK`

```bash
git add deeptutor/services/persona/presets/annotation-coach/PERSONA.md
git commit -m "docs: PERSONA 加知识检索节 (kb_search 依据+来源规则)"
```

---

### Task 4: 冒烟验证

- [ ] **Step 1: 真实检索冒烟**

Run（PowerShell 设 `$env:PYTHONIOENCODING="utf-8"`、`$env:PYTHONPATH=<项目根>`）：

```python
import asyncio
from deeptutor.tools.kb_search_tool import KbSearchTool

async def main():
    tool = KbSearchTool()
    for q in ["遮挡", "IOU", "GB/T 41867", "不存在的内容xyz"]:
        r = await tool.execute(query=q)
        print(f"--- {q}\n{r.content[:200]}\n")

asyncio.run(main())
```

Expected: "遮挡"/"IOU"/"GB/T 41867" 命中并带来源；"不存在的内容xyz" 返回"知识库未收录此内容"。

- [ ] **Step 2: 收尾**

冒烟通过即完成。确认 `git push origin main` 后 `git ls-files data/user/workspace/annotation_kb` 数量正常。
